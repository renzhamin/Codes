#include <iostream>
#include <algorithm>
using namespace std;
int main(){
	freopen("Input.txt","r",stdin); freopen("Output.txt","w",stdout);
	long long n,nt,x;
	int ans;
	while(1){
		ans=0;
		string s,t;
		cin>>s;
		if(s[0]=='.') break;
		n=s.length();
		for(int i=0;i<n;++i){
			t.push_back(s[i]);
			x=n/(i+1);
			string tmp=string("sd",x);
		}		
	}
}